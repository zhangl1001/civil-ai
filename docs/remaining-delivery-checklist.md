# 剩余交付任务清单

> 更新：2026-07-26
> 当前阶段：核心业务链路已收口，剩余内容为真机验收、算法校准和专用题型深化。

## 已完成

- [x] 无默认工程的完整建档、目标/现状/约束/偏好和考试周期。
- [x] SQLite 主存储、IndexedDB fallback、Repository Port、Unit of Work、迁移、索引、乐观锁、幂等和 Outbox。
- [x] IndexedDB 完整事务串行化；全量备份、跨实体完整性校验和单事务恢复。
- [x] 客观题交卷后的画像、复习、计划、主动提醒和 AI 错因通过 Outbox 自动恢复。
- [x] 结构化讲义/题组生成、Schema 校验、质量门禁、原子落库和失败重试。
- [x] OpenAI Compatible、Anthropic 和 DeepSeek Anthropic 兼容适配；不依赖 `response_format`。
- [x] 公共结构化 JSON 提取，兼容纯 JSON、代码围栏和简短前后缀。
- [x] Agent Loop、工具白名单、上下文预算、检查点、调用账本、取消和默认三并发。
- [x] AgentRun 按内容生成、批改诊断、实时交互和后台维护分池调度，并在供应商全局并发上限下降级合并。
- [x] 精确 scope 重复派发限制；限流/瞬断/空响应退避和自适应降并发。
- [x] AgentRun、MessageCenter、铃铛、任务栏、工具执行区和结果跳转统一。
- [x] 任务栏、铃铛和对话框共用单一任务状态 Store；未读计数与批量归档走数据库接口。
- [x] 每日私教练习、自主刷题、历史题组、退出重进、自动下一题、交卷和批改。
- [x] 作答、客观判分、错因候选、AI 错因、学习证据、掌握刷新、复习队列和计划项闭环。
- [x] 交卷期间锁定作答与导航；前台只等待确定性提交，派生计算与 AI 错因由 Outbox 恢复。
- [x] 真机交卷事实改为 SQLite 批量写；数据库锁等待、原生调用和恢复动作有界。
- [x] SQLite 主动治理：启动严格回滚，前后台切换时废弃旧锁队列和连接代次、重建连接、恢复 PRAGMA、执行 `quick_check`，所有 Worker 通过恢复门禁。
- [x] SQLite 被动治理：Bridge 或锁超时后拒绝新操作，连接内恢复失败时有冷却地重建 Web Runtime，不清库、不要求重装。
- [x] SQLite 事务 watchdog：核心事务 20 秒、维护事务 120 秒；超时拒绝后续 SQL 并回滚，回滚失败升级连接恢复。
- [x] 清除事务内全局连接重入；计划项与主动私教信号改为复用当前事务，并用 AST 可靠性检查阻止回归。
- [x] 区分核心显式事务与独立 autocommit 写入；消息状态、主动信号和单条模型审计不再额外占用多语句事务。
- [x] 错题本、筛选、批量操作、闪卡和错题重练读取同一事实。
- [x] 申论生成/批改、面试语音输入/深度点评、每日积累、月报、模考和考点精讲进入统一任务链路。
- [x] Markdown 仅保留渲染；文件型题目、讲义、错题、能力和任务数据已移除。
- [x] Vue 生产构建、移动端冒烟、数据库校验、设计系统检查、iOS 资源同步和 Xcode 模拟器构建。
- [x] Vue 页面直接访问 Repository 已归零，并由架构检查阻止回退。
- [x] iOS SQLite 开启 Keychain secret 和加密连接；Web 保留 IndexedDB 调试回退。

## 真机验收

- [ ] iOS 前后台切换与杀进程后背景、弹层、任务和 SQLite 恢复。
- [ ] Anthropic/OpenAI 实际账号下的限流、取消、重试和三个并发任务隔离。
- [ ] 键盘、安全区、底部导航、对话浮层和长材料拖动手势。
- [ ] 语音权限、录音中断、后台恢复和任务完成跳转。

## 产品深化

- [ ] 目标差距到预测分区间的校准模型，并用真实模考更新置信区间。
- [ ] 长材料多问、资料图表、图推多图分组的专用模板元数据、Fixture 和视觉回归。
- [ ] 申论/面试更细 Rubric、评分校准、追问策略和用户纠正路径。
- [ ] Prompt eval、模拟考生回放、生成质量与响应耗时基准。

## 发布前

- [ ] SQLite/SQLCipher 密钥轮换策略和彻底删除验收。
- [ ] 隐私说明、第三方模型数据提示、麦克风/语音权限文案。
- [ ] Release Archive、IPA 安装验证和 App Store 检查。

下一开发入口是“真机验收”，不再继续改基础数据结构。真机发现的问题应落到现有 Repository、AgentRun、MessageCenter 或公共 UI 组件中修复，禁止新增旁路实现。
