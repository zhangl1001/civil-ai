# 公考辅导 iOS 改造计划

> 历史文档声明（2026-07-14）：本文仅用于核对早期功能清单和 iOS 问题背景。所有新实施决策从 `docs/architecture-index.md` 进入；本文中的后端数据主导、旧数据兼容和旧模型增量改造方案均不再实施。

> 当前 Vue + Capacitor 迁移进度、最新任务队列/AI 运行时改动和后续优化清单，以
> `docs/mobile-vue-capacitor-migration-plan.md` 的 `2026-07-13 Progress Snapshot`
> 为准。本文保留早期 iOS 改造目标和验收标准，用于对照业务完整性。

## 一、改造目标

本次改造聚焦四个目标：

1. 打通 iOS 与后端项目数据，避免两套数据各自运行。
2. 补齐“计划 → 学习 → 批改 → 错题 → 复习 → 计划完成”的业务闭环。
3. 重构页面加载架构，解决脚本覆盖、样式串页和启动慢的问题。
4. 优化移动端页面布局、触控体验、安全性和发布流程。

建议采用“后端数据为主、iOS 本地缓存”的架构。IndexedDB 不再作为唯一数据源，而作为离线缓存使用。

---

## 二、第一阶段：业务止血与数据打通

周期：第 1 周  
优先级：P0

### 1. 建立统一数据访问层

新增统一的 `ProjectRepository`，页面不得直接调用 `LocalStore`。

建议接口包括：

- 获取工程列表
- 读取/写入业务文件
- 获取练习、模考、错题索引
- 上传本地变更
- 拉取服务端增量
- 导入已有后端工程

实现两种数据源：

- `RemoteProjectStore`：访问后端 API
- `LocalProjectCache`：离线缓存和待同步队列

页面只调用 Repository，由 Repository 决定读缓存还是请求后端。

### 2. 首次启动数据迁移

首次升级时：

1. 检测 iOS IndexedDB 旧数据。
2. 检测后端同名工程。
3. 展示迁移预览：工程名、文件数、练习数、最近修改时间。
4. 允许用户选择合并、保留两份或以后处理。
5. 迁移完成后记录 schema 和同步版本。

需要避免同名工程静默覆盖。

### 3. 修复当前确定的业务缺陷

本周必须完成：

- 新建计划时将 `business_model` 写入备考计划。
- 近期练习按“日期 + 模块”分别展示，不再拼接模块名后打开。
- 行测与申论模考使用不同目录和独立考试 ID。
- 模考历史按科目过滤。
- 修复首次首页延迟加载。
- 清理仓库内旧 IPA，确定唯一发布产物目录。

### 验收标准

- 后端已有的“江苏省考2027”可在 iOS 查看。
- iOS 新增一条练习后，后端能够读取。
- 断网时仍可查看已缓存内容。
- 恢复网络后可以自动上传本地变更。
- 行测和申论同一天模考不会相互覆盖。

---

## 三、第二阶段：补齐学习业务闭环

周期：第 2 周  
优先级：P0

### 1. 建立统一学习事件模型

所有学习行为完成后统一产生 `LearningEvent`，例如：

- `diagnosis_completed`
- `practice_graded`
- `essay_graded`
- `review_completed`
- `digest_completed`
- `mock_graded`
- `interview_graded`

事件至少包含：

- 工程和任务 ID
- 日期
- 模块、考点
- 题量、正确率、用时
- 来源页面
- 评分置信度
- 下一步建议
- 幂等 ID

### 2. 建立计划进度归约器

新增 `PlanProgressReducer`，消费学习事件并更新今日计划。页面不再直接修改 `备考计划.json`。

完成规则示例：

- 诊断批改完成 → 诊断任务完成
- 专项批改题数达到目标 → 练习任务完成
- 申论完成批改 → 申论任务完成
- 到期错题完成复习 → 复习任务完成
- 每日积累完成阅读或练习 → 积累任务完成
- 模考完成批改 → 模考任务完成

### 3. 统一批改事务

批改涉及练习文件、统计、能力画像、错题、复习队列和计划进度，必须作为一个业务事务处理。

要求：

- 支持幂等重试
- 中途失败可以恢复
- 不允许只更新练习文件而统计未更新
- 每次批改保留操作日志
- AI 输出必须经过结构校验后才能落库

### 4. 增加提醒闭环

接入 iOS 本地通知：

- 今日计划提醒
- 到期错题提醒
- 连续未学习提醒
- 模考计划提醒

通知点击后应直接进入对应任务，而不是只打开首页。

### 验收标准

完成任意一种任务后，首页和计划页状态立即一致；退出重进后状态不丢失；同一批改结果重复提交不会重复累计统计。

---

## 四、第三阶段：页面架构与性能改造

周期：第 3 周  
优先级：P1

### 1. 移除全局 eval 页面系统

改造为 ES Module 或组件化页面：

- 每个页面拥有独立状态
- 页面提供 `mount()` 和 `unmount()`
- 退出页面时清理监听器、定时器和请求
- 禁止页面函数挂到全局 `window`
- CSS 必须使用页面根节点作用域

优先改造顺序：

1. 首页
2. 刷题
3. 模考
4. 错题本
5. 我的
6. 其他二级页面

### 2. 调整加载策略

- 启动只加载首页。
- 其他标签首次进入时加载。
- Mermaid 仅在存在 Mermaid 内容时加载。
- Markdown 渲染器仅在练习、申论等页面加载。
- AI 工具和 Prompt 在首次打开 AI 面板时加载。
- 包内静态文件使用构建版本号缓存，不使用时间戳禁用缓存。

### 3. 建立结构化索引

新增：

- `practice_index`
- `mock_index`
- `wrong_item_index`
- `review_due_index`
- `learning_event_index`

错题本、历史记录和日历直接查询索引，不再扫描全部 Markdown 文件。

### 性能目标

- 冷启动有效首屏低于 1.5 秒
- 标签切换低于 150ms
- 首页加载不超过 20 次数据读取
- WebContent 稳态内存低于 45MB
- 错题达到 5000 条时仍可流畅分页
- 页面退出后无残留定时器和重复监听器

---

## 五、第四阶段：UI、无障碍与安全改造

周期：第 4 周  
优先级：P1

### 1. 首页信息架构

首页只保留：

1. 考试倒计时和当前阶段
2. 今日任务
3. 到期复习
4. 能力摘要
5. 一个主要行动按钮

移除与底栏重复的刷题、模考、错题本快捷入口。“更多工具”迁移到“我的”或独立工具页。

今日计划需要控制模块分布，避免同一天前四项全部属于资料分析。

### 2. 主页面调整

刷题页：

- 智能推题作为首要操作
- 专项练习作为第二层
- 历史记录折叠或独立页面

模考页：

- 行测、申论历史彻底分开
- 展示未完成、待批改、已完成状态
- 支持继续考试

错题本：

- 顶部最多保留两个主要操作
- 筛选放入统一筛选面板
- 默认突出“今天到期”
- 长列表分页或虚拟滚动

我的页面：

- 增加同步状态、最后同步时间
- 数据管理增加系统分享、文件导出
- AI 配置与普通设置分区

### 3. 移动端规范

- 所有可点击区域不小于 44×44pt
- 正文不小于 14px，辅助文字不小于 12px
- 支持系统动态字体
- 允许系统辅助缩放
- AI 悬浮球自动避让底栏和主要按钮
- 图标按钮必须提供无障碍名称
- 深浅色模式通过统一主题系统实现

### 4. 安全改造

- API Key 存入 iOS Keychain
- 禁止把 Key 明文写入 localStorage
- ATS 仅为明确的本地地址放行
- 外部 API 默认要求 HTTPS
- 增加 CSP
- AI 生成内容使用成熟 HTML Sanitizer
- 数据导入前校验 schema、大小、路径和内容类型

---

## 六、测试与发布要求

### 自动化测试

增加以下测试：

- 数据迁移和冲突合并测试
- 六类计划任务完成测试
- 行测/申论模考隔离测试
- 批改幂等和中断恢复测试
- IndexedDB 升级测试
- Repository 离线与重连测试
- XCTest 启动测试
- XCUITest 核心用户路径

核心端到端路径：

1. 导入工程
2. 生成今日计划
3. 完成诊断
4. 提交批改
5. 错题进入复习队列
6. 完成错题复习
7. 今日计划变为完成
8. 后端和 iOS 数据一致

### 发布治理

- 保留一个 IPA 输出目录
- 构建产物写入版本号、提交号和构建时间
- CI 自动校验 IPA 内资源与源码一致
- 禁止提交 DerivedData、archive 和临时 IPA
- 发布前运行业务回归、性能基准和数据迁移演练

---

## 七、建议里程碑

- M1：后端数据可在 iOS 正常展示
- M2：六类学习任务全部可以自动闭环
- M3：移除全局 eval，完成五个主页面模块化
- M4：冷启动低于 1.5 秒
- M5：完成 UI、Keychain、通知和自动化测试
- M6：灰度发布，验证旧数据迁移与同步稳定性

整个改造过程中，优先保证数据不丢、任务状态一致，再进行视觉重构。建议按“Repository → 事件闭环 → 页面架构 → UI”的顺序逐步替换。
