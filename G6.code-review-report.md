# G6 代码审计报告

- **项目**：`zhangl-agent`
- **审计范围**：iOS 原生插件、Capacitor 边界、Agent Runtime、聊天 Agent、Provider Gateway、任务恢复、记忆/上下文、测试与发布配置
- **仓库**：`/Users/zhanglei/code/zhangl-agent`
- **分支**：`release/baseline-v1.1.0`
- **审计日期**：2026-07-29
- **审计方式**：源码追踪、架构文档对照、静态配置检查、执行现有 verifier，并对高风险发现进行独立复核

## 一、总体结论

当前系统已经具备一个**受控 Agent Runtime 原型**：有多轮 loop、工具注册、Provider 适配、技能、预算、checkpoint、任务状态和部分恢复机制；iOS 也已经接通 Keychain、SQLite、流式网络、相机、OCR、语音和本地通知插件。

但它目前还**不符合“可托付的、可恢复的本地 AI 私教 Agent”规范**，主要原因不是缺少模型能力，而是关键的确定性边界没有在代码层闭合：

1. **租约没有 fencing**，旧 Worker 失去租约后仍可能提交状态、checkpoint 或副作用。
2. **写工具没有持久化 receipt/idempotency boundary**，崩溃窗口会导致恢复重放。
3. **取消、确认、schema、资源归属和完成判定没有统一由 Runtime 强制执行**。
4. **上下文中存在不可信文本进入 system prompt 的路径**，并且 Context Compiler 尚未真正装配。
5. **iOS 存在已提交私钥、任意目标原生 HTTP、资源无界、语音生命周期不完整等问题**。
6. **现有测试大多是 fake/mock 或静态检查；全部通过不能证明真实 iOS、真实 Provider 和真机恢复可用**。

### 质量门禁结论

- **P0：1 项，必须在继续扩大 Agent 能力前修复。**
- **P1：13 项，当前不建议发布或让 Agent 执行高影响业务操作。**
- **P2：12 项，建议在真机验收和发布前完成。**
- **P3：3 项，建议纳入发布自动化。**
- 当前 Gate **不通过**：存在未修复 P0/P1。

### Agent 成熟度

| 维度 | 评级 | 结论 |
|---|---:|---|
| Runtime 框架能力 | **L3- / 58 分** | loop、tool、skill、Provider、checkpoint 等骨架真实存在；但租约 fencing、写入幂等、统一取消和运行时授权边界缺失。 |
| 实际业务智能 | **L2 / 35 分** | 能调用聊天、生成、研究和学习工具；但完成判定部分依赖模型提示，无法稳定区分“已受理/运行中/失败/已提交”，高影响动作也未按成本和影响分级确认。 |
| 综合 | **46 / 100** | “受控 Agent 框架原型”，还不是足够可靠、足够安全、足够智能的自治私教。 |

这里的“智能”不是只看模型能否生成文本，而是看它能否在真实环境中**规划、执行、观察、验证、恢复，并且不把未完成工作说成完成**。当前框架有规划循环，但确定性控制面仍不完整。

## 二、P0：必须立即修复

### P0-1：租约失效后旧 Worker 仍可执行并提交结果

**位置**：

- `web/src/modules/agent/application/RunTutorAgentBatch.ts:78-85,122-130`
- `web/src/modules/agent/application/ClaimAgentRuns.ts:47-50`
- `web/src/modules/agent/adapters/SqliteAgentRunRepository.ts:144-156`
- `web/src/modules/agent/application/TransitionAgentRun.ts:12-18`
- `web/src/modules/agent/application/SaveAgentLoopCheckpoint.ts:16-49`

**证据**：

- `renewLease()` 返回 `boolean`，但 `ClaimAgentRuns.renew()` 丢弃结果；heartbeat 只 `catch(() => undefined)`。
- 租约过期恢复只清空 `lease_owner/lease_expires_at`，重新排队。
- checkpoint、进度和状态转换只依赖 `runId/version`，没有要求当前 `workerId`、租约代次或仍有效的租约。

**触发场景**：浏览器后台节流、模型/工具调用超过 lease、数据库短暂错误或 heartbeat 迟到，使 Worker A 失去租约；恢复器将同一 run 交给 Worker B；A 随后恢复并继续执行。

**影响**：A 与 B 可同时调用 Provider、执行工具、写 checkpoint、发送 Outbox、完成/失败同一 AgentRun。对生成、研究、发布、写业务事实等有副作用工具，可能发生重复执行、错误状态覆盖和数据污染。这直接违反架构文档“旧代次事务不得在恢复后提交”的要求（`docs/architecture-constitution.md:149-153`）。

**修复建议**：

1. 在持久化记录中加入单调递增 `leaseEpoch`/fencing token。
2. claim 生成新 epoch；checkpoint、progress、transition、最终提交和 Outbox 配对写入都强制校验 `(runId, workerId, leaseEpoch, leaseExpiresAt > now)`。
3. `renewLease === false` 或 heartbeat 抛错时立即 abort 当前 signal，并禁止后续 Provider/tool/状态写入。
4. 恢复 Worker 使用新 epoch；旧 epoch 只能读取，不能提交。
5. 增加两个 Worker 的并发失租约测试，断言旧 Worker 的最终提交被拒绝。

## 三、P1：当前必须修复

### P1-1：OTA TLS 私钥已提交并与证书匹配

**位置**：`ios/App/ota/key.pem:1-28`；关联 `ios/App/ota/cert.pem`。

**触发场景/影响**：任何当前或历史仓库读取者均可取得私钥。若该证书继续用于 OTA HTTPS 服务，可冒充 OTA 服务并篡改 manifest/传输内容；私钥暴露不可通过删除工作树撤销。

**复核**：`git ls-files` 确认该文件被跟踪；通过 OpenSSL 比对，key 与 cert 公钥匹配。

**修复建议**：立即轮换/吊销旧证书和私钥；迁移到 CI Secret/受控密钥库；从当前工作树移除并按凭据泄露流程处理 Git 历史；核查已安装客户端和 OTA 服务是否仍依赖旧证书。

### P1-2：原生流式 HTTP 允许任意目的地、方法、请求头和重定向

**位置**：`ios/App/App/NativeStreamingHTTPPlugin.swift:34-59,81-118`；`ios/App/App/config.xml:3`。

**触发场景**：任意可调用 Capacitor Bridge 的 JS 传入任意 URL、HTTP method、headers 和 body。

**影响**：没有 scheme/host/port/IP 段、DNS 解析、重定向目标校验或 method/header allowlist。若不可信页面/脚本进入 Bridge 信任边界，设备或 VPN 网络可被用来访问内网、环回、链路本地和管理接口；响应 headers/body 也会回传 JS。

**修复建议**：原生层建立精确 HTTPS host allowlist；拒绝 loopback、link-local、私网和保留 IP；关闭自动重定向并逐跳验证；限制 method/header；不要回传不必要的敏感响应头；收紧 WebView 导航与 Bridge 信任边界。

### P1-3：工具成功与 checkpoint 持久化之间存在崩溃重复执行窗口

**位置**：`web/src/modules/agent/application/RunAgentLoop.ts:433-553`；`web/src/modules/agent/contracts/AgentRuntimePorts.ts:98-138`。

**触发场景**：工具已经创建任务、生成内容或完成外部操作，但进程在工具结果 checkpoint 写入前被杀死、存储失败或租约被回收。

**影响**：成功工具签名只保存在内存；恢复时看不到成功调用，会再次执行写工具，重复创建题组、研究任务、草稿或业务事实。

**修复建议**：dispatch 前持久化 `tool_call_started`；唯一键使用 `agentRunId + providerCallId`；将 receipt/idempotency key 强制传入 Tool Executor 和 Application Use Case；恢复先查询 receipt/目标资源再决定读取、补偿或继续，不能直接重放。

### P1-4：高成本/高影响业务写工具默认自动执行

**位置**：`web/src/services/ChatAgentCapabilities.ts:20-29`；`web/src/services/AIBusinessToolCatalog.ts:47-145`；`web/src/modules/agent/application/DefaultAgentToolPolicy.ts:10-23`。

**证据**：聊天业务工具统一标为 `AgentToolRisk.Write` 且 `requiresConfirmation: false`；默认 Policy 只对 `Destructive` 或显式确认工具返回 Confirm。

**触发场景**：模型、网页内容或污染上下文触发大题量生成、整卷生成、联网研究、月报或高 token 任务。

**影响**：用户未明确确认即产生较大成本、后台任务和内容资产，违反 `docs/architecture-constitution.md:164-180` 与 ADR 对高影响动作需代码裁决的要求。

**修复建议**：用风险矩阵取代二元 Write/Destructive：按题量、预计 token、联网范围、持久化影响和是否可撤销决定 allow/confirm/reject；确认时冻结参数哈希，恢复时复验。

### P1-5：Tool Schema、资源授权和目标归属没有统一运行时强制校验

**位置**：`web/src/modules/agent/domain/AgentToolRegistry.ts:24-61`；`web/src/modules/agent/application/DefaultAgentToolPolicy.ts:10-23`；`web/src/modules/agent/application/RegisteredAgentToolExecutor.ts:24-33`；`web/src/services/ChatAgentCapabilities.ts:20-29`。

**证据**：Registry 只检查工具元数据；Policy 只检查 `_parseError`、风险和确认标志；Executor 直接将模型参数交给 handler。没有统一执行 JSON Schema 的 required/enum/range/additionalProperties，也没有统一校验 session/exam-cycle/resource ownership。

**触发场景**：模型传错类型、附加字段、伪造资源 ID，或提示注入诱导跨会话/跨考试周期操作。

**影响**：每个下游 handler 被迫自行承担 schema、权限、归属和确认边界；任一遗漏就可能错误写入或越权。

**修复建议**：在 Policy 前建立 `ToolInvocationValidator`：标准 JSON Schema、branded ID、session/exam-cycle/resource ownership、风险和预算一次性裁决；Executor 只接收已验证的命令对象。

### P1-6：原生 Anthropic Gateway 固定发送 temperature，当前模型可能直接 400

**位置**：`web/src/modules/agent/application/RunAgentLoop.ts:261-267`；`web/src/modules/agent/application/InvokeAgentModel.ts:133-142`；`web/src/capabilities/ai-runtime/providers/AnthropicGateway.ts:167-175`。

**触发场景**：Agent 默认/透传 `temperature: 0.2`，用户选择拒绝该 sampling 参数的当前原生 Anthropic 模型。

**影响**：Provider 400，AgentRun 进入失败/重试，原生 Claude 配置不可用。

**修复建议**：在 Provider Adapter 内维护模型 capability matrix；对不支持的模型省略 `temperature/top_p/top_k`，使用该模型支持的 thinking/effort 参数。不能把协议差异泄漏到业务 loop。

### P1-7：Task Dock 取消无法中断直连 Chat Agent

**位置**：`web/src/components/TaskDock.vue:211-214`；`web/src/modules/agent/application/CancelAgentRun.ts:14-25`；`web/src/modules/agent/application/RunTutorAgentBatch.ts:122-129`；`web/src/services/ChatAgentService.ts:110-117,337-383`。

**触发场景**：用户取消正在流式输出、等待 Provider 或执行工具的聊天任务。

**证据**：Task Dock 持久化取消；共享 `AgentRunExecutionRegistry` 的 controller 由 batch worker 注册，而 Chat Agent 使用 `active.controller` 直接调用 loop，没有注册到共享 registry。

**影响**：数据库/UI 显示 cancelled，但 stream、后续 model turn 或 tool 仍可能继续，继续消耗成本并产生副作用。

**修复建议**：以 `agentRunId` 统一管理 controller；Chat Agent 注册到 `AgentRunExecutionRegistry`，所有 UI/API 取消路径复用同一 signal；取消后 Provider、工具和 assistant 持久化均需有确定性终态。

### P1-8：内外层重试叠加，一次 Provider turn 最多放大为 16 次调用

**位置**：`web/src/modules/agent/application/InvokeAgentModel.ts:215-279`；`web/src/modules/agent/application/RunTutorAgentBatch.ts:146-160`；`InvokeAgentModel.ts:258-261`。

**触发场景**：429、短暂网络故障或 Provider overload。

**影响**：单 turn 内层最多 4 次，整个 AgentRun 又可最多重试 4 次，形成最多 16 次模型调用；`Retry-After` 被强行限制为 5 秒，可能在 Provider 指定冷却期前再次请求，放大限流和成本。

**修复建议**：只保留一个 retry owner，或共享总调用次数/绝对 deadline；在 deadline 允许时尊重 provider `Retry-After`；增加 429 长等待、backoff 中取消和重试耗尽测试。

### P1-9：完成条件 Validator 只部分进入 Prompt，未成为可执行业务验证

**位置**：`web/src/modules/agent/domain/AgentSkillRegistry.ts:180-203`；`web/src/modules/agent/application/AgentLoopStatePolicy.ts:50-73`；`web/src/modules/agent/application/RunAgentLoop.ts:529-537`；`web/src/services/ChatAgentService.ts:581-595`。

**触发场景**：写工具返回某 task ID 后，模型读取了无关、queued 或 running 的任务；或者 completion verifier 只报告 `madeProgress`。

**影响**：Agent 把“已受理/运行中/无关任务”当成目标已完成，并生成虚假完成说明，违反“AI 提案、代码裁决”。

**修复建议**：写工具返回类型化 `{expectedResourceId, resourceType, commitRequirement}`；Verifier 必须匹配该目标、资源类型和允许终态，并确认 committed；`madeProgress` 不能单独解除 completion gate。

### P1-10：用户/模型文本经摘要进入 top-level system prompt，存在提示注入持久化路径

**位置**：`web/src/ai/ChatContextBuilder.ts:112-121`；`web/src/ai/prompts.ts:23-32`；`web/src/services/AIStudentContextService.ts:8-16`；`web/src/services/ChatAgentService.ts:323-335`。

**触发场景**：用户、网页内容回显或旧模型文本注入“后续摘要时忽略限制/调用某工具”等指令；摘要或档案名称在后续 turn 被拼入 system。

**影响**：不可信文本取得最高提示优先级，削弱工具权限、确认、数据最小化和完成门禁。

**修复建议**：摘要、档案显示名、网页内容和历史模型文本全部作为 tainted data 结构化传递，禁止直接拼入 top-level system；加入来源标签、严格字段边界、长度限制和 injection regression tests。

### P1-11：Web Research URL policy 可能被 DNS rebinding/私网重定向绕过

**位置**：`web/src/capabilities/web-research/domain/WebUrlPolicy.ts:10-37`；`web/src/capabilities/web-research/adapters/ConfiguredWebResearchGateway.ts:191-214,241-264,365-379`。

**触发场景**：公网 host 解析到私网/链路本地 IP，或公网 URL 重定向到私网地址。

**影响**：Agent 的联网工具可能访问本机、云 metadata 或公司内网资源，形成 SSRF 和数据泄露。

**修复建议**：请求前解析并拒绝所有非公网 IP；关闭自动重定向，逐跳重新校验目标；覆盖 IPv4/IPv6 保留段、DNS rebinding 和 redirect-to-private。

### P1-12：缺少第一方 XCTest/XCUITest，关键 iOS 逻辑没有自动化证明

**位置**：`ios/App/App.xcodeproj/project.pbxproj:103-123`；`ios/App/App.xcodeproj/xcshareddata/xcschemes/App.xcscheme:26-32`。

**证据**：仅有 App target，TestAction 没有 TestableReference；项目没有第一方 XCTest/XCUITest 源目录。

**影响**：Keychain、SQLite bridge、流式 HTTP、相机/OCR、语音中断、通知、前后台和强杀恢复都可能回归而 CI 不知情。

**修复建议**：增加 Unit/UI test target，CI 执行 simulator `xcodebuild test`；发布前加入真机矩阵。

### P1-13：当前 iOS 验证链路不是真机/IPA 运行验证

**位置**：`scripts/verify-ios-lifecycle-recovery.mjs:19-35,95-154`；`scripts/verify-ios-ipa.js:21-54`；`scripts/smoke-vue-mobile.js:40-70`；`package.json:32-33`。

**证据**：生命周期测试使用 fake lifecycle/timer/database/runtime；IPA 校验只解压检查资源；mobile smoke 检查构建产物、route 字符串和 import 禁令；`npm test` 实际走 `smoke:ios -> smoke:vue`，不安装、不启动 IPA。

**影响**：所有验证通过仍无法证明启动白屏、插件注册、SQLite 初始化、权限弹窗、安全区、键盘、手势、后台恢复和真实 Provider 传输可用。

**修复建议**：archive 后自动 install/launch；用本地 HTTP/SSE contract server 驱动真实 transport；用 simulator/真机验证 background → active、强杀重启、数据库恢复和核心 UI。

## 四、P2：应在真机验收/发布前修复

### P2-1：Context Compiler 没有生产装配，预算只覆盖部分聊天文本

**位置**：`web/src/modules/agent/contracts/AgentRuntimePorts.ts:71-83`；`web/src/services/AgentConversationMemoryService.ts:71-97`；`web/src/modules/agent/application/AgentLoopSupport.ts:135-162`。

**问题**：system、工具 schema、skill workflow、图片、工具结果和最大输出预留未统一纳入同一 Provider/model budget；长会话和多工具任务可能上下文超限或关键内容被截断。

**修复**：实现真正的 render-time Context Compiler，按模型能力预算 tools/system/memory/messages/media/output，并记录真实 usage 校准。

### P2-2：Memory forget 只写 tombstone，原文仍在共享日志

**位置**：`web/src/modules/agent/adapters/FileAgentMemoryRepository.ts:70-85,93-109,120-146`。

**问题**：遗忘只在回放时隐藏旧记录，包含原文的日志仍在本地文件、备份或导出中。

**修复**：按 session/scope 分文件并加密；遗忘执行物理删除或受控压缩重写，并定义备份保留期。

### P2-3：预算耗尽保存 checkpoint，却被 Chat Agent 转为不可恢复失败

**位置**：`web/src/modules/agent/application/RunAgentLoop.ts:244-253`；`web/src/services/ChatAgentService.ts:377-379,421-432`；`web/src/modules/agent/domain/AgentRunMachine.ts:32-39`。

**问题**：UI 文案称“进度已保留，可继续”，但 run 已进入 `failed`，状态机只允许从 `waiting_user` resume。

**修复**：增加 `paused_budget/resumable`，或从 checkpoint 创建严格关联的 continuation run；否则修改文案，不能承诺不可用的恢复。

### P2-4：IndexedDB 多标签页缺少跨 runtime CAS/幂等保护

**位置**：`web/src/capabilities/database/adapters/indexeddb/TutorIndexedDb.ts:120-131,226-231`；`web/src/modules/agent/adapters/IndexedDbAgentRunRepository.ts:19-27`。

**问题**：read-then-add、版本检查和 put 不能跨 tab 原子保护；单 runtime 内存队列无法保护另一个 tab，可能重复 AgentRun、丢状态转换或覆盖 checkpoint。

**修复**：在同一 read-write transaction 内 claim/CAS；增加 durable unique idempotency index，并用 `navigator.locks/BroadcastChannel` 选举单一跨 tab worker。

### P2-5：分片生成首个失败不会取消 sibling Provider 请求

**位置**：`web/src/modules/content/application/ShardedObjectiveGenerator.ts:82-105,486-505`；`web/src/modules/content/application/GenerationModelInvoker.ts:53-88`；`RunStructuredObjectiveGenerationWorkflow.ts:245-247,566-607`。

**问题**：一个 shard 失败后其他并行请求仍继续；workflow 已失败时未完成 invocation 可能留下永久 pending ledger，持续消耗成本。

**修复**：fan-out 建立 child AbortController；首个不可恢复失败立即 abort，随后 `Promise.allSettled`，所有 invocation 在失败提交前进入终态。

### P2-6：流式 HTTP 没有并发、总字节、事件数和背压上限

**位置**：`ios/App/App/NativeStreamingHTTPPlugin.swift:20-22,54-65,111-118,154-157`。

**问题**：每个 chunk Base64 后异步投递主线程/WebView；没有全局/来源并发、累计 bytes、事件数、队列深度或速率上限，长流和大量并发会导致主线程积压、内存增长或 WebView 被杀。

**修复**：设定全局及每来源限制、最大持续时间/字节/事件数；使用有界缓冲、合并和取消策略。

### P2-7：文档/OCR 在限制前进行完整大内存分配

**位置**：`ios/App/App/NativeDocumentTextPlugin.swift:20-30,64-88,126-142`。

**问题**：先完整 Base64 解码再检查 25 MB；PDF 先全量聚合文本再截断 500,000 字符，恶意输入可造成 OOM 或长时间阻塞。

**修复**：先按 Base64 长度拒绝明显超限输入；逐页维护剩余字符预算，达到上限立即停止；增加图像像素、单页文本和 autorelease pool 限制。

### P2-8：Agent 工作区持久化写入和读取无配额/分页

**位置**：`ios/App/App/NativeAgentWorkspacePlugin.swift:16-37,40-57`。

**问题**：无单行、单文件、文件数、总目录配额；`read` 整文件加载并回传。重复 append 或大量 logKey 可耗尽磁盘/内存。

**修复**：限制 key/line/file/total bytes，rotation/截断、TTL 清理，read 按 byte/line 分页并返回明确配额错误。

### P2-9：语音识别结束/失败/后台中断没有统一 teardown

**位置**：`ios/App/App/SpeechRecognitionPlugin.swift:54-60,80-91,118-128`；`ios/App/App/AppDelegate.swift:42-56`。

**问题**：最终回调仅停止 engine/remove tap；完整清理（endAudio、cancel task/request、deactivate AVAudioSession）只在显式 stop，后台/中断/错误路径可能残留 record/duckOthers。

**修复**：成功、失败、取消、音频中断、resign-active/background 全部进入幂等 teardown，并向 JS 发送确定事件。

### P2-10：缺少一方 Privacy Manifest

**位置**：未发现 `ios/App/App/PrivacyInfo.xcprivacy`；target 配置在 `ios/App/App.xcodeproj/project.pbxproj:103-123`。

**问题**：项目使用 UserDefaults、Keychain、相机、麦克风、语音/OCR 等 API；Info.plist usage description 不能替代 Required Reason API 和数据实践声明，可能阻塞 App Store 审核。

**修复**：添加并版本控制 app-owned `PrivacyInfo.xcprivacy`，同步 App Store Connect 隐私标签、模型数据提示和权限说明。

### P2-11：Release export 依赖被忽略且当前为 development 配置

**位置**：`scripts/archive-ios-web.sh:29-33`；`build/ios/ExportOptions.plist:5-16`；`.gitignore:10`。

**问题**：干净 clone/CI 中 export 依赖被忽略的 build 文件，且 `method=development`，不能作为 App Store/TestFlight 发布配置。

**修复**：把不含私密信息的配置模板纳入版本控制或由 CI 生成；显式区分 development/OTA/App Store 配置并检查目标环境。

### P2-12：SwiftPM 声明与 resolved 版本不一致

**位置**：`ios/App/CapApp-SPM/Package.swift:14`；`ios/App/App.xcodeproj/project.xcworkspace/xcshareddata/swiftpm/Package.resolved:5-11`。

**问题**：声明固定 `capacitor-swift-pm` 8.4.1，而 lock 记录 branch 8.0.0；新机器、清 DerivedData 或 archive 时可能漂移/失败，构建不可复现。

**修复**：重新生成 Capacitor SPM 配置，提交一致的 lock 状态，并在 CI 校验解析后工作树无变化。

## 五、P3：建议纳入发布自动化

### P3-1：通知在系统异步接受前即报告成功

**位置**：`ios/App/App/LearningNotificationPlugin.swift:46-65`。

`UNUserNotificationCenter.add` 未使用 completion handler，代码在系统确认前递增并 resolve；调度失败时 UI 仍可能显示成功。应汇总每项 completion 结果后再返回。

### P3-2：活跃 HTTP stream 没有后台/杀进程终态契约

**位置**：`ios/App/App/NativeStreamingHTTPPlugin.swift:14-22,54-65,160-169`；`ios/App/App/AppDelegate.swift:42-56`。

stream 仅存在内存字典和 ephemeral URLSession，后台挂起/杀进程后没有持久化终态、取消事件或恢复 reconciliation。应明确前台-only 取消契约，或改用合适 background session 并持久化状态。

### P3-3：OTA manifest 版本依赖人工同步

**位置**：`ios/App/ota/manifest.plist:21-25`；`ios/App/App.xcodeproj/project.pbxproj:339,362`。

提升 `MARKETING_VERSION` 后可能遗漏 manifest，导致 OTA 展示和安装版本元数据漂移。应由 archive 自动生成，或 CI 比较并失败。

## 六、现有防线与边界

审查确认以下防线目前有效，但不能抵消上述问题：

- `KeychainPlugin.swift:15-89` 使用 bundle-scoped service 和 `kSecAttrAccessibleAfterFirstUnlockThisDeviceOnly`；未发现硬编码 API key。
- `NativeAgentWorkspacePlugin.swift:78-97` 将 logKey 编为 Base64URL 文件名，未发现 `../` 路径逃逸。
- `NativeCameraPlugin.swift:19-56,76-163` 在 capture 前请求权限，并限制 JPEG 尺寸/分辨率。
- `Info.plist:29-36` 已配置相机、麦克风、相册、语音识别 usage descriptions。
- 未发现任意 ATS 放宽；但 ATS 不能替代 HTTP host/redirect allowlist。
- HTTP stream map 有锁保护，正常终止会 invalidate session；问题主要是资源上限和后台语义。
- Web/SQLite/Outbox 等架构骨架与 `docs/architecture-constitution.md` 的分层方向大体一致。

## 七、测试结果与测试可信度

本次实际执行：

```text
node scripts/verify-agent-loop.mjs       PASS
node scripts/verify-agent-runtime.mjs    PASS
node scripts/verify-provider-gateway.mjs PASS
node scripts/verify-ios-lifecycle-recovery.mjs PASS
```

但这些通过结果必须正确解读：

- Agent loop/runtime verifier 使用 fake model、fake executor、fake repository 或 fake clock，不能证明真实工具副作用、租约竞争和 SQLite CAS。
- Provider verifier 使用注入的 `send()` stub 和 synthetic Response，不经过真实 `PlatformHttpTransport`/Capacitor 原生传输，不覆盖真实认证、SSE 分帧、重定向、超时和当前 Provider 参数兼容性。
- iOS lifecycle verifier 使用 fake lifecycle/timer/database/runtime，不是 simulator/真机生命周期。
- IPA verifier 只解压比较资源，不 install/launch。
- `smoke-vue-mobile.js` 是构建产物/字符串/import 检查，不挂载 Vue、不点按、不验证键盘、安全区、手势或任务跳转。
- 根 `npm test` 通过 `package.json:32-33` 走 `smoke:ios -> smoke:vue`，名称不能被解读为 iOS 真机测试。

## 八、建议修复顺序

### 第一阶段：先封住一致性和安全边界

1. 轮换 OTA 私钥/证书，并清理历史暴露。
2. 引入 lease fencing，旧 Worker 的所有提交必须被拒绝。
3. 为写工具建立持久化调用账本、receipt 和幂等键。
4. 统一 Chat Agent 与后台 Worker 的 cancellation registry/signal。
5. 原生 HTTP host/IP/redirect allowlist；Web research 同步修复 DNS/redirect SSRF。
6. 统一 ToolInvocationValidator，强制 schema、资源归属、确认、预算和参数哈希。

### 第二阶段：修复 Agent 的“智能可靠性”

1. 将完成条件从 prompt 文本改为代码可验证的目标资源终态。
2. 对生成、研究、批改、发布按成本/影响实施分级确认。
3. 修复 Anthropic model capability matrix 和 retry ownership。
4. 完成 Context Compiler；将 system、tools、memory、messages、media、output 统一预算。
5. 将预算耗尽建模为可恢复状态；实现遗忘的物理删除/受控压缩。

### 第三阶段：真机和发布验收

1. 新建 XCTest/XCUITest target，执行 `xcodebuild test`。
2. archive 后 install/launch IPA，覆盖冷启动、前后台、强杀、SQLite/Keychain 恢复。
3. 增加真实 transport 的本地 HTTP/SSE contract tests；真实 Provider nightly smoke。
4. 覆盖键盘、安全区、语音中断、手势、任务完成跳转和 VoiceOver。
5. 添加 Privacy Manifest，修复 export 配置和 SwiftPM lock 漂移。

## 九、审计限制

- 仓库没有 `G2.tec-design-detail.md`，也没有有效的项目 `knowledge-base/INDEX.md`；本审计以 `docs/architecture-constitution.md`、`docs/adr-001-provider-neutral-agent-runtime.md`、`docs/remaining-delivery-checklist.md` 等项目文档为设计参照。
- 审计期间未修改任何源代码；工作区原有未提交变更为：`QuestionSwipeNavigation.ts`、`TutorPracticeCenterView.vue` 及两个 PracticeMode 文件，本报告不将其误归为本次审计产生的变更。
- 本报告未把无法从当前源码和调用链独立确认的外部线索列为正式问题；正式条目均有仓库内文件、行号和触发条件。
